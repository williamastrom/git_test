
  # Echo Viewer prototype

  This is a code bundle for Echo Viewer prototype. The original project is available at https://www.figma.com/design/3ACqn27CInLQe0lpyP07GX/Echo-Viewer-prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  